{{#reveal-modal id="reboot-server-modal" hasButtons="true" ok="rebootServer"}}
    {{#each model in models}}
        <p>Are you sure you want to reboot the VM <strong>{{model.name}}</strong>?</p>
    {{/each}}
{{/reveal-modal}}